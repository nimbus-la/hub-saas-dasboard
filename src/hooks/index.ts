/**
 * Hooks compartidos
 *
 * Los que no son de ninguna funcionalidad en concreto. Un hook que sólo usa
 * productos vive en `features/products/hooks`; éste es el sitio de lo que
 * cualquier pantalla puede necesitar.
 */

export * from "./use-debounced-value";
export * from "./use-pagination";
