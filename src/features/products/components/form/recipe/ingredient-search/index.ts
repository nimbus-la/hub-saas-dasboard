// ── Buscador de insumos ─────────────────────────────────────────────────────
// El campo con el que se añade un insumo a la receta. `IngredientSearchResult`
// no sale de esta carpeta: es cómo se pinta una fila del desplegable, y quien
// lo usara suelto se llevaría el resultado sin la búsqueda que lo produce.
export { default as IngredientSearchField } from './IngredientSearchField';
